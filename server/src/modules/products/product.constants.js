const PRODUCT_TYPES = {
  BEVERAGE: 'BEVERAGE',
  FOOD: 'FOOD',
  CAKE: 'CAKE',
  BAKED_ITEM: 'BAKED_ITEM',
  COMBO: 'COMBO',
};

const INVENTORY_MODES = {
  RECIPE_BASED: 'RECIPE_BASED',
  STOCK_BASED: 'STOCK_BASED',
};

const SELECTION_TYPES = {
  SINGLE: 'SINGLE',
  MULTIPLE: 'MULTIPLE',
};

const PRODUCT_TYPE_VALUES = Object.values(PRODUCT_TYPES);
const INVENTORY_MODE_VALUES = Object.values(INVENTORY_MODES);
const SELECTION_TYPE_VALUES = Object.values(SELECTION_TYPES);

module.exports = {
  PRODUCT_TYPES,
  INVENTORY_MODES,
  SELECTION_TYPES,
  PRODUCT_TYPE_VALUES,
  INVENTORY_MODE_VALUES,
  SELECTION_TYPE_VALUES,
};
